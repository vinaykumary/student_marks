class Student < ActiveRecord::Base
  belongs_to :department
  has_many :marks
  attr_accessible :Department, :Exam, :Mark
    attr_protected :id
  scope :dept_sem, lambda{ |dept,sem| { :conditions => ["department_id = ? and semester = ? ORDER BY roll_no ", dept, sem] }}
end
